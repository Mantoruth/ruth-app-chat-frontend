 /*eslint-disable no-unused-vars */

 export default class StubRtcClient {
    // Suppress ESLint warnings for unused variables in method parameters
    /* eslint-disable no-unused-vars */
  
    on(event, callback) {
      // Stub the event handling
    }
  
    initialize(localVideoElement) {
      // Stub the initialization process
    }
  
    toggleMute(isMuted) {
      // Stub the mute/unmute functionality
    }
  
    disconnect() {
      // Stub the disconnection process
    }
  
    shareScreen() {
      // Stub the screen sharing functionality
    }
  
    sendChatMessage(message) {
      // Stub the chat message sending
    }
  
    /* eslint-enable no-unused-vars */
  }
  